<article>
  <header>
    <h1>
      <a href="<%= url(article.slug) %>"><%= article.title %></a>
    </h1>
    <%= article.date %>
    </time>
  </header>
  <section class="content">
    <%= markdown article.content %>
  </section>
</article>
